
/*
 * DO NOT EDIT THIS FILE!
 * It is generated automatically by ctpdb
 */

#if defined(HAVE_PRAGMA_ONCE)
#pragma once
#endif

#ifndef SOUND_RECORD_H__
#define SOUND_RECORD_H__

class SoundRecord;

#include "CTPDatabase.h"
#include "CTPRecord.h"
class CivArchive;

#define k_Num_SoundRecord_Tokens 2


class SoundRecord : public CTPRecord
{
public:
    typedef sint32 (SoundRecord::*IntAccessor)() const;
    typedef bool   (SoundRecord::*BoolAccessor)() const;
    typedef double (SoundRecord::*FloatAccessor)() const ;
    typedef bool   (SoundRecord::*BitIntAccessor)(sint32 &val) const;
    typedef bool   (SoundRecord::*BitFloatAccessor)(double &val) const;
    typedef sint32 (SoundRecord::*IntArrayAccessor)(sint32 index) const;
    typedef double (SoundRecord::*FloatArrayAccessor)(sint32 index) const;

private:
    char *            m_Value;
    char *            m_NameText;
    bool m_hasGovernmentsModified; // GovMod specific flag

public:
    SoundRecord() { Init(); };
    SoundRecord(CivArchive &archive) { Serialize(archive); };
    SoundRecord(SoundRecord const & rval) { Init(); *this = rval; }
    ~SoundRecord();
    SoundRecord const & operator = (SoundRecord const & rval);

    void Init();
    void Serialize(CivArchive &archive);

    void CheckRequiredFields(DBLexer *lex);
    sint32 Parse(DBLexer *lex, sint32 numRecords);

    void ResolveDBReferences();
    void Merge(const SoundRecord & rval);

    char const * GetValue() const { return m_Value; }
    char const * GetNameText() const { return m_NameText; }
    bool GetHasGovernmentsModified() const { return m_hasGovernmentsModified; }
    sint32 GenericGetNumGovernmentsModified() const { return 0; }
    sint32 GenericGetGovernmentsModifiedIndex(sint32 index) const { return -1; }

}; /* SoundRecord */

struct SoundRecordAccessorInfo
{
    SoundRecord::IntAccessor        m_intAccessor;
    SoundRecord::BoolAccessor       m_boolAccessor;
    SoundRecord::FloatAccessor      m_floatAccessor;
    SoundRecord::BitIntAccessor     m_bitIntAccessor;
    SoundRecord::BitFloatAccessor   m_bitFloatAccessor;
    SoundRecord::IntArrayAccessor   m_intArrayAccessor;
    SoundRecord::FloatArrayAccessor m_floatArrayAccessor;
};

extern SoundRecordAccessorInfo      g_SoundRecord_Accessors[];
extern CTPDatabase<SoundRecord> *   g_theSoundDB;

extern const char * g_Sound_Tokens[];

#endif
